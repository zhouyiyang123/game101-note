#### __Lesson 2 __线性代数复习 \(Review of Linear Algebra\)

#### 1\.向量 \(Vectors\)图形学中通常默认使用列向量。

2\.点乘 \(Dot Product\)：计算公式：__a__·__b__ = ||__a__|| ||__b__||。

图形学应用：

寻找两个向量之间的夹角（计算光照时至关重要）。

计算一个向量在另一	个向量上的投影。

3\.叉乘 \(Cross Product\)：结果：得到一个同时垂直于 __a__ 和 __b__ 的新向量，方向符合右手定则（__a __x __b__方向用右手握拳，四指从__a__到__b__，大拇指发现为叉乘后方向）。

图形学应用：

建立三维坐标系。判断左/右：若__a __x __b__的z轴分量为正，则__b__在__a__的左侧。

判断内/外：用来检测点是否在三角形内部，与每条边依次叉乘，若全为左/右，则	在内侧（光栅化的核心算法）

4\.矩阵 \(Matrices\)

矩阵乘法：用于将各种变换（旋转、缩放、平移）复合在一起，不满足交换律（AB≠BA）。

#### __Lesson 3  __变换 \(Transformation\)

#### 1\. 2D 线性变换 \(Linear Transformations\)

#### 通常的 2D 几何变换可以通过一个 2 × 2 的矩阵与 2D 向量相乘来实现

#### 缩放 \(Scaling\)： = 

#### 旋转 \(Rotation\)：绕原点逆时针旋转角度： = 

#### 2\. 齐次坐标 \(Homogeneous Coordinates\)

#### 为什么需要齐次坐标：因为线性变换无法表示平移，平移不能写成单一矩阵相乘。

#### 解决方案：引入第三个维度w。

#### 2D 点表示为： 

#### 向量表示为：  （向量具有平移不变性，所以w = 0可以保护向量不因平移矩	阵而改变）。

#### 平移： = 

#### 3\. 复合变换：

#### 图形学中物体的变换往往是“先旋转、再平移”。写成矩阵乘法时，从右向左依次作用于	点：

复合变换结果： = 

#### 4\. 三维变换：

#### 点表示为： 

向量 表示为：  

变换类似二维

#### __Lecture 04:__ 三维观测变换 \(3D Viewing Transformations\)

#### 1\.视图/相机变换 \(View/Camera Transformation\)

#### 物理类比：拍电影的第一步——布置相机和调整相机位置。

#### 如何定义一个相机？ 需要三个向量：

#### 位置__e__

#### 观测方向 

#### 向上方向 

#### 核心思想：移至原点

#### 为了简化计算，图形学中有一个铁律：永远让相机位于原点\(0,0,0\)，看向\-Z轴，头	顶朝向\+Y轴。

#### 如果相机不在这里，我们就把相机和场景里的所有物体一起移动，直到相机回到这	个标准位置（相对运动，相对位置保持不变）。

#### 变换矩阵的推导：

#### 直接求相机怎么过去很难，但求从标准原点到相机当前位置的逆变换很容易。求出	逆矩阵后，利用正交矩阵的转置等于逆（）的特性，可以瞬间写出。

#### 2\.投影变换 \(Projection Transformation\)

#### 把 3D 的世界压扁成 2D 图像。

#### 投影分为两类：

#### A\. 正交投影 \(Orthographic Projection\)

#### 特点：没有“近大远小”的现象，光线是平行的。多用于工程制图或 2D 轴侧游	戏。

#### 数学操作：

#### 定义一个空间立方体，由左, 右\(l, r\)、下, 上\]\(b, t\)、远, 近\]\(f, n\) 六个面组成。

#### 平移，把这个立方体的中心移到坐标原点。

#### 缩放，把这个立方体缩放到一个标准的单位立方体，即的空间。

#### \(注：因为相机看向\-Z，所以近的坐标值大于远的坐标值。\)

#### B\. 透视投影 \(Perspective Projection\)

#### 特点：符合人类视觉，近大远小；平行线在远方会相交于一点（灭点）。它的可视	区域是一个视锥体 \(Frustum\)。

#### 如何做透视投影：

#### 第一步（挤压）：把远大近小的“视锥体”挤压成一个“长方体”。

#### 第二步（正交投影）：直接套用上面的正交投影矩阵。

#### 挤压矩阵的推导核心（相似三角形）：

#### 利用相似三角形原理，可以算出挤压后任何一个点的新的x和y坐标。

#### 根据“近平面上的点在挤压后完美保持不变”和“远平面的中心点在挤压后保持	不变”这两个边界条件，推导出整个挤压矩阵。

#### 在这个挤压过程中，原本位于远近平面正中间的点的Z轴坐标会向远平面靠拢	（即Z变大了），这意味着挤压并不是线性的。
